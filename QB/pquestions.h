extern char* readTextFile(char path);
extern char** testCode(int *completed, char *path, char *expectedOut, int lastAttempt, char *expectedImage);
extern char** compileCode(int* completed, char* question, char* code, int lastAttempt);
